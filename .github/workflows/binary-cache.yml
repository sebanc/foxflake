name: Build FoxFlake binary cache and update flake locks 
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'
  push:
    branches:
      - 'main'
    paths-ignore:
      - 'README.md'
concurrency:
  group: environment-${{ github.ref }}
  cancel-in-progress: true
jobs:
  create-foxflake-binary-cache:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - id: stable
            name: "stable branch"
          - id: stable-test
            name: "stable-test branch"
          - id: unstable
            name: "unstable branch"
          - id: unstable-test
            name: "untable-test branch"
          - id: dev
            name: "dev branch"
    steps:
      - name: Initialize matrix lock
        if: matrix.id == stable
        uses: rakles/matrix-lock@v1
        with:
          step: init
          order: "stable,stable-test,unstable,unstable-test,dev"
      - name: Reclaim disk space
        uses: wimpysworld/nothing-but-nix@main
        with:
          hatchet-protocol: 'holster'
      - name: Display disk space
        run: |
          echo "Free space:"
          df -h
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: main
      - name: Install nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
      - name: Install cachix action
        uses: cachix/cachix-action@v16
        with:
          name: foxflake
          authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
      - name: Build FoxFlake binary cache
        run: |
          ./create_binary_cache.sh ${{ matrix.id }}
      - name: Wait for matrix lock
        uses: rakles/matrix-lock@v1
        with:
          step: wait
          id: ${{ matrix.id }}
      - name: Upload updated FoxFlake flake.lock
        uses: actions/upload-artifact@v4
        with:
          name: foxflake-${{ matrix.id }}-flake-lock
          path: /home/runner/work/foxflake/foxflake/foxflake-${{ matrix.id }}-flake.lock
          if-no-files-found: error
      - name: Continue matrix lock
        uses: rakles/matrix-lock@v1
        with:
          step: continue
          id: ${{ matrix.id }}
  update-foxflake-flake-locks:
    needs: create-foxflake-binary-cache
    permissions:
      contents: write
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - id: stable
          - id: stable-test
          - id: unstable
          - id: unstable-test
          - id: dev
    steps:
      - name: Checkout ${{ matrix.id }} branch
        uses: actions/checkout@v4
        with:
          ref: ${{ matrix.id }}
      - name: Download FoxFlake ${{ matrix.id }} flake lock
        uses: actions/download-artifact@v4
        with:
          name: foxflake-${{ matrix.id }}-flake-lock
          path: /home/runner/work/foxflake/foxflake/
      - name: Copy FoxFlake ${{ matrix.id }} flake lock
        run: |
          cp ./foxflake-${{ matrix.id }}-flake.lock ./flake.lock
          rm ./foxflake-${{ matrix.id }}-flake.lock
      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          branch: ${{ matrix.id }}
          commit_message: Update flake lock
